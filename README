Pour lancer le projet:
- npm install, puis ng serve dans le dossier frontend
- npm install, puis npm start OU nodemon server dans le dossier backend

Puis, se rendre sur un navigateur (idéalement Chrome) et rentrer l'url http://localhost:4200
