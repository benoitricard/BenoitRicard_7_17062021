Pour lancer le projet:

Dans le dossier FRONTEND
=> npm install
=> ng serve

Dans le dossier BACKEND
=> npm install
=> npm start

Puis, se rendre dans un navigateur (idéalement Chrome) à l'adresse suivante => http://localhost:4200
